# Tools and Shared Resources Module

This module groups shared code, model files and utility scripts referenced by examples.

Included folders (logical references):

- `ModelFiles`
- helper libraries and shared utilities

Notes:
- Keep large binary models out of the main branch if possible; provide links or releases instead.
